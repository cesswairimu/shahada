<div class="top-bar">
    <div class="top-bar-right">
        <h5> <%= link_to "About", "#about" %></h5>
        <h5><%= link_to "Contact", "#footer" %></h5>
        <% if logged_in? %>
        <h5><%= link_to "Logout", logout_path, method: :delete %></h5>
    <% elsif signed_in? %>
        <h5><%= link_to "Signout", signout_path, method: :delete %></h5>
        <% else %>
        <h5><%= link_to "Login", log_in_path %></h5>
        <% end %>
    </div>
    <div class="top-bar-left">
        <%# <%= link_to  image_tag("logo1.png"), root_url%> 
            <h2><%= link_to "Shahada", root_url %></h2>
    </div>
</div>
