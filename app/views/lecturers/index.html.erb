 <h2> Lecturers </h2>
<table>
    <thead>
        <tr>
            <th> No.</th>
            <th>First Name </th>
            <th>Last Name </th>
            <th>School </th>
        </tr>
    </thead>
    <% @lecturers.each do |user| %>
        <tr>
            <td><%= user.id %></td>
            <td> <%= user.f_name%></td>
            <td><%= user.l_name %> </td>
            <td> <%=user.school %></td>
        <% end %>
        </tr>
</table>
